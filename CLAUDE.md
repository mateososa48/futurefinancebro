@AGENTS.md

## Deployment Rules

- **Never run `vercel deploy` or `vercel --prod`** — Vercel is connected to GitHub and deploys automatically on every push.
- **Deploy = `git push` only.**
- **Always ask before pushing.** Do not push/deploy automatically after making changes — confirm with the user first.
